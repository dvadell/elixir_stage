----
title: Documentation & open-questions resolution
----

Update the project docs to describe the new LLM conversation architecture and
record the decisions this epic resolved. No functional code changes.

## Scope

### 1. `sdlc/TECHNICAL_DESCRIPTION.md`

- §1 architecture diagrams: the `Soundai.Conversation` seam now relays the
  transcript to an LLM through `branched_llm` (ReqLLM) instead of echoing; add
  the audio-mode path (`/api/conversations/audio` → LLM → `Soundai.TTS` → WAV).
- §3 routes table: add `POST /api/conversations/audio` (WAV + headers) and note
  the `soundai_conversation` cookie + `fetch_cookies` on the `:api` pipeline.
- New subsection on the LLM layer: `branched_llm` dep (git tag), NVIDIA
  provider config, `openai/gpt-oss-20b` default model, in-memory
  `Soundai.Conversation.Store` (TTL), system prompt, `max_tokens`/length caps,
  adapter injection for tests, and the LLM error vocabulary (T0016).
- §12 gotchas: add cookie/`fetch_cookies` note, LLM timeout budget, and the
  "server engine now means audio reply" convention.
- Repository layout: new files (`conversation/store.ex`, `conversation/llm.ex`,
  `conversation_audio_controller.ex`, tickets).

### 2. `README.md` — Status

- Move Milestone 2 / "LLM conversation (Needle)" items: mark implemented items
  from this epic ([x]) and adjust the "What works" flow to show the LLM
  response instead of the echo. Note the reply modes (server = audio, native/
  local = text). Keep the offline-first framing accurate (STT local; the LLM
  round trip is online-only).
- Update the TTS/ticket pointers and add the new tickets T0012…T0016 to the
  docs section.

### 3. `sdlc/PRD.md` — open questions

- Mark Q2 (Web Worker) resolved as before; add resolutions for this epic:
  - Q5 "How should conversation history be persisted?" → active-session only,
    in-memory per conversation_id with TTL; persistence remains future.
  - Q9/model/provider → NVIDIA OpenAI-compatible endpoint,
    `openai/gpt-oss-20b`, via `branched_llm`.
  - Note the "Needle" references: the orchestration layer is now
    `branched_llm` (wrapper around ReqLLM); keep the OpenAI-compatible
    commitment in §6.4.
- §20/§21: no metric changes required, but add the LLM-latency + LLM-failure
  metrics to the §21 lists.

### 4. Dependency notes

- Add a note about the `branched_llm` version pinned in `mix.lock` and the
  BL tickets (in `branched_llm/sdlc/tickets/`) that the pin depends on.

## Acceptance criteria

- [x] TECH_DESCRIPTION accurately describes the LLM relay, audio mode, cookie
      flow, and error vocabulary (review against the code at epic end).
- [x] README status checkboxes reflect reality after T0012…T0016.
- [x] PRD open questions updated with the resolved decisions.
- [x] No functional code changes in this ticket.

## Verification

- Read-through review of the three docs against the implemented behavior.
- `mix precommit` (umbrella root) still passes (docs only, but the alias runs
  format/credo/dialyzer/tests — cheap sanity check).

## Done

Docs only (no code changes):

- `TECHNICAL_DESCRIPTION.md`: §1 rewritten — audio-mode and text-mode paths,
  and a new §1.3 "The LLM layer" (branched_llm git dep tag v0.3.1 pinned in
  `mix.lock` + BL-01…03, NVIDIA provider config, default `openai:gpt-oss-20b`,
  `Soundai.Conversation.Store` TTL, system prompt, `max_response_chars` cap,
  adapter injection, error vocabulary). §12 gotchas gained the
  cookie/`fetch_cookies` note, the LLM timeout budget, the "server engine now
  means the audio reply" convention, and the never-raise seam rule. Routes
  table, error/HTTP table, latency budget, layout and client-state sections were
  already updated in T0016.
- `README.md` (updated in T0016): What works shows the LLM + both reply modes;
  Status adds the LLM conversation milestone and marks the epic items; ticket
  pointers cover T0017/T0012…T0016.
- `PRD.md`: §20 "Phoenix + Needle" decision now notes `branched_llm` as the
  orchestration layer (OpenAI-compatible commitment in §6.4 kept); §21 metrics
  call out LLM latency (504/timeout) and LLM failures by reason (502/504);
  §22 open questions: Q5 (in-memory session-only history with TTL), Q6
  (interrupt = new press + watchdog), Q8 (STT offline, LLM online-only),
  Q10 (out of scope, PRD §14), new Q11 (model/provider → NVIDIA +
  `openai/gpt-oss-20b` via `branched_llm`).

Checks (umbrella root): format, credo --strict, `mix test` (87 soundai + 19
soundpanel) — all green. No Elixir/JS code touched in this ticket.